<p style="color: green"><%= notice %></p>
<%= @medical_record.patient.first_name %>
<%= render @medical_record %>

<div>
  <%= link_to "Edit this medical record", edit_patient_medical_record_path(params[:patient_id]) %> |
  <%= link_to "Back to medical records", patient_medical_record_path%>
 <%= button_to "Destroy this medical record", patient_medical_record_path(params[:patient_id], @medical_record), method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
